# Stripe Payment Link. Create at https://dashboard.stripe.com/payment-links
# Set to the full https://buy.stripe.com/... URL
NEXT_PUBLIC_STRIPE_PAYMENT_LINK=

# Direct download URL for the notarized DMG (GitHub Releases or Cloudflare R2)
# e.g. https://github.com/YOUR_USERNAME/Tappit/releases/download/v1.0.0/Tappit-1.0.0.dmg
NEXT_PUBLIC_DOWNLOAD_URL=

# The waitlist / pre-launch CTA is configured at runtime via a Cloudflare Pages
# Function (functions/api/cta.ts) that reads CTA_MODE, WAITLIST_URL, and
# PURCHASE_URL from Pages env vars. Set those in the Pages dashboard or in the
# root wrangler.toml — no build-time env var is needed for the waitlist URL.
